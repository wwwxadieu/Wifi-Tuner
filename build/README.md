# build/

Thư mục tài nguyên đóng gói (`buildResources` của electron-builder).

TODO (Giai đoạn 4 — hoàn thiện): thêm `icon.ico` (Windows) và `icon.png` tại đây, sau đó khai báo `win.icon` / cửa sổ `icon` trong `package.json` và `electron/main.js`. Hiện tại chưa có icon riêng nên bản build dùng icon mặc định của Electron.
